#include <stdio.h>
#define MAX 2000
#define INFINITO 999999999

int main(){
    int n, lista[MAX], matriz[MAX][MAX];
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
        scanf("%d", &lista[i]);
    return 0;
}
